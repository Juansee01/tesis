name: "f1_analytics"
version: "1.0.0"
config-version: 2

profile: "f1_analytics"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  f1_analytics:
    gold:
      materialized: table
      schema: gold
